pipeline {
agent any
stages {
        stage('deploy') {
         steps {
            echo "deploying"
            }
            when {
          branch 'master'
          }
          }
        }
    }
